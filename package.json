{
  "name": "painel-contratos-seduc",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start": "npm run start -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
